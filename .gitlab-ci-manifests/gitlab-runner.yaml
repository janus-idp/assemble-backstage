apiVersion: apps.gitlab.com/v1beta2
kind: Runner
metadata:
  name: gitlab-runner
spec:
  gitlabUrl: https://gitlab.consulting.redhat.com/
  token: gitlab-runner-secret
  tags: backstage
  #config: ocp-config-toml