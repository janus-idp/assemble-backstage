# Default values for quarkus-build.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  org: dummy
  name: dummy
  commit_sha: dummy

quay:
  username: dummy
  token: dummy